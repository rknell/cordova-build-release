{
  "name": "cordova-build-release",
  "version": "0.0.2",
  "description": "CLI utility to build Android & IOS release packages in one command",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Snappy Apps",
  "license": "ISC",
  "preferGlobal": "true",
  "bin": {
    "cordova-build": "app.js"
  },
  "dependencies": {
    "q": "^1.0.1"
  }
}
